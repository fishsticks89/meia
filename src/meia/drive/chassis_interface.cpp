#include "main.h"
namespace meia {
    // void MovementTelemetry::wait_until(double val) {
    //     bool break_allowed = false;
    //     while (!break_allowed) {
    //         mutex->take(8000);
    //         if (*current_id != id)
    //             break_allowed = total_move - val > total_move - *amount_completed;
    //         else
    //             break_allowed = true;
    //         mutex->give();
    //         if (!break_allowed)
    //             pros::delay(10);
    //     }
    //     std::cout << "command delivered" << std::endl;
    //     return;
    // }
}